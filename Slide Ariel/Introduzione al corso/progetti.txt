Design and deployment of the university network (with IMUNES). The university consists of three sections (administration, professors, students), two student labs, five research labs. Design the university network and propose a suitable deployment using layers 2 and 3 approaches (VLAN, trunking, IP...)
- IMUNES

Implementation of a router in c 
- c
- funzionalità/protocolli di routing
- modulo kernel space o quagga

Implementation of a P2P Gnutella-like client
- c

Implementation of P2P chord protocol using the socket library
- c

Design and deployment of a network consisting of:
- A DHCP server distributing IP addresses
- A DNS server translating domain names in IP addresses
- A choice of two: FTP server, HTTP server, SMTP server
- An internal and an external user
- IMUNES

Design and deployment of a network consisting of:
- A DHCP server distributing IP addresses
- A DNS server translating domain names in IP addresses
- A transparent proxy for filtering and load balancing, mediating requests to an HTTP server
- An internal and an external user
- IMUNES

Design and deployment of a network whose requests from/to internal machines (both clients and servers) are mediated by a Linux firewall (e.g., IPTABLES)
- IPTABLES

Design and deployment of a network consisting of:
- A DHCP server distributing IP addresses
- A DNS server translating domain names in IP addresses
- A HTTP server providing virtual hosting and SSL connections
- A HTTP proxy server
- An internal and an external user
- IMUNES

Implementation of an application protocol of your choice using socket API
- c

Study and practical demonstration of HTTP/3

SNMP test and demonstration

Implement of traceroute using the socket